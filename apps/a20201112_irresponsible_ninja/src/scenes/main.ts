export default class extends Phaser.Scene {
    constructor() {
        super({
            key: 'MainScene',
            active: true
        });
    }

    preload(): void {
        // this.load.sceneFile('FootScene', footScene);
    }

    // create(): void {
        
    // }
}